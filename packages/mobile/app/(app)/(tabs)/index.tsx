import { Screen, Text } from "../../../src/ui/components";

/** Home tab — placeholder shell screen. */
export default function Home() {
  return (
    <Screen centered>
      <Text variant="h1">Divvy Up</Text>
    </Screen>
  );
}
